"use client";
import React from "react";
import ScrollableComponent from "../components/ScrollableComponent";

const ScrollPage = () => {
  return (
    <div>
      <h1>Scrollable Container with Buttons</h1>
      <ScrollableComponent />
    </div>
  );
};

export default ScrollPage;
